<div class="min-h-screen bg-gray-50 py-12 px-4 sm:px-6 lg:px-8">
  <div class="max-w-4xl mx-auto">
    <div class="text-center mb-8">
      <h2 class="text-3xl font-extrabold text-gray-900">
        処理状況一覧
      </h2>
      <p class="mt-2 text-sm text-gray-600">
        現在のCSV処理状況を確認できます
      </p>
    </div>

    <% if flash[:notice] %>
      <div class="mb-4 bg-green-50 border border-green-200 text-green-700 px-4 py-3 rounded">
        <%= flash[:notice] %>
      </div>
    <% end %>

    <div class="bg-white shadow overflow-hidden sm:rounded-md">
      <% if @processing_statuses.any? %>
        <ul class="divide-y divide-gray-200">
          <% @processing_statuses.each do |status| %>
            <li>
              <div class="px-4 py-4 sm:px-6">
                <div class="flex items-center justify-between">
                  <div class="flex items-center">
                    <div class="flex-shrink-0">
                      <div class="h-10 w-10 rounded-full bg-blue-100 flex items-center justify-center">
                        <svg class="h-6 w-6 text-blue-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z"></path>
                        </svg>
                      </div>
                    </div>
                    <div class="ml-4">
                      <div class="flex items-center">
                        <p class="text-sm font-medium text-gray-900">
                          トークン: <%= status.token %>
                        </p>
                        <div class="ml-2">
                          <% if status.done_count == status.total_count %>
                            <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-green-100 text-green-800">
                              完了
                            </span>
                          <% elsif status.done_count > 0 %>
                            <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-yellow-100 text-yellow-800">
                              処理中
                            </span>
                          <% else %>
                            <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-gray-100 text-gray-800">
                              待機中
                            </span>
                          <% end %>
                        </div>
                      </div>
                      <div class="mt-1 flex items-center text-sm text-gray-500">
                        <p>
                          進捗: <%= status.done_count %> / <%= status.total_count %>
                          (<%= number_to_percentage(status.total_count > 0 ? (status.done_count.to_f / status.total_count * 100) : 0, precision: 1) %>)
                        </p>
                      </div>
                      <div class="mt-1 text-sm text-gray-500">
                        <p>キュー: <%= status.queue_name %></p>
                        <p>作成日時: <%= status.created_at.strftime("%Y-%m-%d %H:%M:%S") %></p>
                      </div>
                    </div>
                  </div>
                  <div class="flex items-center">
                    <%= link_to "詳細", csv_processing_show_path(status.token), 
                        class: "text-blue-600 hover:text-blue-500 text-sm font-medium" %>
                  </div>
                </div>
                
                <!-- プログレスバー -->
                <div class="mt-3">
                  <div class="bg-gray-200 rounded-full h-2">
                    <div class="bg-blue-600 h-2 rounded-full" 
                         style="width: <%= status.total_count > 0 ? (status.done_count.to_f / status.total_count * 100) : 0 %>%">
                    </div>
                  </div>
                </div>
              </div>
            </li>
          <% end %>
        </ul>
      <% else %>
        <div class="text-center py-12">
          <svg class="mx-auto h-12 w-12 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z"></path>
          </svg>
          <h3 class="mt-2 text-sm font-medium text-gray-900">処理状況がありません</h3>
          <p class="mt-1 text-sm text-gray-500">新しいCSVファイルをアップロードしてください。</p>
        </div>
      <% end %>
    </div>

    <div class="mt-6 text-center">
      <%= link_to "新しいCSVファイルをアップロード", new_csv_processing_path, 
          class: "inline-flex items-center px-4 py-2 border border-transparent text-sm font-medium rounded-md text-white bg-blue-600 hover:bg-blue-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-blue-500" %>
    </div>
  </div>
</div> 