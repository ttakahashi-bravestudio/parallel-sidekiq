<div class="min-h-screen bg-gray-50 py-12 px-4 sm:px-6 lg:px-8">
  <div class="max-w-md mx-auto">
    <div class="text-center">
      <h2 class="mt-6 text-3xl font-extrabold text-gray-900">
        CSVファイル処理
      </h2>
      <p class="mt-2 text-sm text-gray-600">
        CSVファイルをアップロードして並列処理を開始します
      </p>
    </div>

    <div class="mt-8">
      <% if flash[:alert] %>
        <div class="mb-4 bg-red-50 border border-red-200 text-red-700 px-4 py-3 rounded">
          <%= flash[:alert] %>
        </div>
      <% end %>

      <%= form_with url: csv_processing_index_path, method: :post, local: true, class: "space-y-6" do |form| %>
        <div>
          <label for="csv_file" class="block text-sm font-medium text-gray-700">
            CSVファイル
          </label>
          <div class="mt-1">
            <%= form.file_field :csv_file, 
                accept: '.csv', 
                required: true,
                class: "block w-full text-sm text-gray-500 file:mr-4 file:py-2 file:px-4 file:rounded-full file:border-0 file:text-sm file:font-semibold file:bg-blue-50 file:text-blue-700 hover:file:bg-blue-100" %>
          </div>
          <p class="mt-2 text-sm text-gray-500">
            ヘッダー行を含むCSVファイルを選択してください
          </p>
        </div>

        <div>
          <label for="csv_type" class="block text-sm font-medium text-gray-700">
            処理タイプ
          </label>
          <div class="mt-1">
            <%= form.select :csv_type, 
                options_for_select([
                  ['xlsx', 'xlsx'],
                  ['pdf', 'pdf']
                ]),
                {},
                class: "mt-1 block w-full pl-3 pr-10 py-2 text-base border-gray-300 focus:outline-none focus:ring-blue-500 focus:border-blue-500 sm:text-sm rounded-md" %>
          </div>
        </div>

        <div>
          <%= form.submit "処理開始", 
              class: "group relative w-full flex justify-center py-2 px-4 border border-transparent text-sm font-medium rounded-md text-white bg-blue-600 hover:bg-blue-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-blue-500" %>
        </div>
      <% end %>
    </div>

    <div class="mt-6 text-center">
      <%= link_to "処理状況を確認", csv_processing_status_path, 
          class: "text-blue-600 hover:text-blue-500 text-sm" %>
    </div>
  </div>
</div> 